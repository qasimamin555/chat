// import redux from 'redux'
// import reactRedux from 'react-redux'

  var initial_state = {
      name:"qasim amin",
      age:'20'
  }
  export default (state = initial_state) => {
      return state;
  }